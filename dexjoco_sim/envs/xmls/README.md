The full `dexjoco_sim/envs/xmls` asset tree is distributed as a GitHub release asset instead of staying in Git.

From the repository root, restore it with:

```bash
python scripts/download_assets.py --bundle sim-xmls
```
